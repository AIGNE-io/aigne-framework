export const DEFAULT_RUNTIME_ID = "default-runtime";

export const DEFAULT_RETRY_DELAY = 3 * 1000; // 3 seconds

export const DEFAULT_COOLDOWN_PERIOD = 3 * 60 * 1000; // 3 minutes

export const DEFAULT_MAX_CONSECUTIVE_FAILURES = 3;
