type: array
items:
  type: object
  properties:
    title:
      type: string
    description:
      type: string
    path:
      type: string
      description: 路径，以 RUL 的格式返回，不能为空, 必须以 / 开头，不需要包含语言层级，比如 /zh/about 直接返回 /about
    parentId:
      type:
        - string
        - 'null'
  required:
    - title
    - description
    - path